# Mihomo (Clash Meta) — headless client for the handbook bot.
# Copied into the image as /app/mihomo/config.yaml (see Dockerfile).
# Ports are offset from v2rayN defaults so both can run on the same host:
#   v2rayN  SOCKS/HTTP  10808 / 10809
#   mihomo  mixed       11808
#   mihomo  API         19090
#
# Apply: docker compose up -d --build
#
# Add a VLESS/SS subscription: duplicate a subN block (name / url / path / prefix)
# and append it under FAST.use (curated) or BACKUP.use (pool). Do not put
# whitelist CIDR/SNI on the bot path.
#
# FAST is url-test of sub3: sit on the lowest Telegram RTT among curated nodes.
# BACKUP is fallback of sub1/sub2: on a dead FAST node, try the next leaf
# immediately instead of waiting for a full url-test pass. AUTO is fallback
# FAST→BACKUP so a healthy FAST keeps the fast node; a dead FAST group walks
# BACKUP on the same request.
# Provider pulls use SUBSCRIBE: DIRECT first, AUTO if GitHub is blocked.
# Do not point providers at AUTO alone — that deadlocks when the tunnel is down.
# Cached provider YAML still works if both DIRECT and AUTO fail.

mixed-port: 11808
bind-address: 127.0.0.1
allow-lan: false
mode: rule
log-level: info
ipv6: false
unified-delay: true
tcp-concurrent: true
keep-alive-interval: 15
find-process-mode: off
geodata-mode: false
geo-auto-update: false
global-client-fingerprint: chrome

external-controller: 127.0.0.1:19090
secret: "8176598712630598761082765412765789012506456781928765078960"

profile:
  store-selected: true
  store-fake-ip: false

dns:
  enable: true
  ipv6: false
  enhanced-mode: redir-host
  nameserver:
    - 8.8.8.8
    - 1.1.1.1
  fallback:
    - tls://1.1.1.1
    - tls://8.8.8.8
  fallback-filter:
    geoip: false

proxy-providers:
  sub1:
    type: http
    url: "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/BLACK_VLESS_RUS.txt"  #Черные Списки VLESS / Все конфиги
    path: ./providers/sub1.yaml
    interval: 900 # раз в 15 мин 3600-час
    size-limit: 10485760
    proxy: SUBSCRIBE
    header:
      User-Agent:
        - "clash.meta"
        - "mihomo"
        - "clash-verge"
    health-check:
      enable: true
      url: https://www.gstatic.com/generate_204
      interval: 300
      timeout: 4000
      lazy: true
      expected-status: 204
    override:
      additional-prefix: "s1 | "
      udp: true
    exclude-filter: "(?i)expire|expiring|traffic|remain|剩余|流量|过期|官网|套餐|到期|重置|订阅"

  sub2:
    type: http
    url: "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/BLACK_SS%2BAll_RUS.txt" #Черные Списки Shadowsocks+All / Все конфиги
    path: ./providers/sub2.yaml
    interval: 900
    size-limit: 10485760
    proxy: SUBSCRIBE
    header:
      User-Agent:
        - "clash.meta"
        - "mihomo"
        - "clash-verge"
    health-check:
      enable: true
      url: https://www.gstatic.com/generate_204
      interval: 300
      timeout: 4000
      lazy: true
      expected-status: 204
    override:
      additional-prefix: "s2 | "
      udp: true
    exclude-filter: "(?i)expire|expiring|traffic|remain|剩余|流量|过期|官网|套餐|到期|重置|订阅"

  sub3:
    type: http
    url: "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/BLACK_VLESS_RUS_mobile.txt" #Черные Списки VLESS / 150 лучших для телефона
    path: ./providers/sub3.yaml
    interval: 900
    size-limit: 10485760
    proxy: SUBSCRIBE
    header:
      User-Agent:
        - "clash.meta"
        - "mihomo"
        - "clash-verge"
    health-check:
      enable: true
      url: https://www.gstatic.com/generate_204
      interval: 300
      timeout: 4000
      lazy: true
      expected-status: 204
    override:
      additional-prefix: "s3 | "
      udp: true
    exclude-filter: "(?i)expire|expiring|traffic|remain|剩余|流量|过期|官网|套餐|到期|重置|订阅"

  sub4:
    type: http
    url: "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/WHITE-CIDR-RU-all.txt" #Белые Списки / CIDR / Все конфиги
    path: ./providers/sub4.yaml
    interval: 3600
    size-limit: 10485760
    proxy: SUBSCRIBE
    header:
      User-Agent:
        - "clash.meta"
        - "mihomo"
        - "clash-verge"
    health-check:
      enable: true
      url: https://www.gstatic.com/generate_204
      interval: 300
      timeout: 4000
      lazy: true
      expected-status: 204
    override:
      additional-prefix: "s4 | "
      udp: true
    exclude-filter: "(?i)expire|expiring|traffic|remain|剩余|流量|过期|官网|套餐|到期|重置|订阅"

  sub5:
    type: http
    url: "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/WHITE-SNI-RU-all.txt"  #Белые Списки / SNI
    path: ./providers/sub5.yaml
    interval: 3600
    size-limit: 10485760
    proxy: SUBSCRIBE
    header:
      User-Agent:
        - "clash.meta"
        - "mihomo"
        - "clash-verge"
    health-check:
      enable: true
      url: https://www.gstatic.com/generate_204
      interval: 300
      timeout: 4000
      lazy: true
      expected-status: 204
    override:
      additional-prefix: "s5 | "
      udp: true
    exclude-filter: "(?i)expire|expiring|traffic|remain|剩余|流量|过期|官网|套餐|到期|重置|订阅"

  # sub6:
  #   type: http
  #   url: "REPLACE_ME_SUB6"
  #   path: ./providers/sub6.yaml
  #   interval: 3600
  #   size-limit: 10485760
  #   proxy: SUBSCRIBE
  #   header:
  #     User-Agent:
  #       - "clash.meta"
  #       - "mihomo"
  #       - "clash-verge"
  #   health-check:
  #     enable: true
  #     url: https://www.gstatic.com/generate_204
  #     interval: 600
  #     timeout: 4000
  #     lazy: false
  #     expected-status: 204
  #   override:
  #     additional-prefix: "s6 | "
  #     udp: true
  #   exclude-filter: "(?i)expire|expiring|traffic|remain|剩余|流量|过期|官网|套餐|到期|重置|订阅"

  # sub7:
  #   type: http
  #   url: "REPLACE_ME_SUB7"
  #   path: ./providers/sub7.yaml
  #   interval: 3600
  #   size-limit: 10485760
  #   proxy: SUBSCRIBE
  #   header:
  #     User-Agent:
  #       - "clash.meta"
  #       - "mihomo"
  #       - "clash-verge"
  #   health-check:
  #     enable: true
  #     url: https://www.gstatic.com/generate_204
  #     interval: 600
  #     timeout: 4000
  #     lazy: false
  #     expected-status: 204
  #   override:
  #     additional-prefix: "s7 | "
  #     udp: true
  #   exclude-filter: "(?i)expire|expiring|traffic|remain|剩余|流量|过期|官网|套餐|到期|重置|订阅"

  # sub8:
  #   type: http
  #   url: "REPLACE_ME_SUB8"
  #   path: ./providers/sub8.yaml
  #   interval: 3600
  #   size-limit: 10485760
  #   proxy: SUBSCRIBE
  #   header:
  #     User-Agent:
  #       - "clash.meta"
  #       - "mihomo"
  #       - "clash-verge"
  #   health-check:
  #     enable: true
  #     url: https://www.gstatic.com/generate_204
  #     interval: 600
  #     timeout: 4000
  #     lazy: false
  #     expected-status: 204
  #   override:
  #     additional-prefix: "s8 | "
  #     udp: true
  #   exclude-filter: "(?i)expire|expiring|traffic|remain|剩余|流量|过期|官网|套餐|到期|重置|订阅"

proxy-groups:
  # Real Bot API: GET /bot is 404 JSON. 200/301/302 matches DPI redirects.
  # Provider health-checks still use gstatic for general liveness.
  # sub4/sub5 are whitelist — they look fast on Google/DPI and cannot carry
  # Bot API; keep them off the bot path.
  - name: FAST
    type: url-test
    use:
      - sub3
    url: https://api.telegram.org/bot
    interval: 120
    timeout: 4000
    tolerance: 100
    lazy: false
    max-failed-times: 1
    expected-status: 404
    exclude-filter: "(?i)expire|expiring|traffic|remain|剩余|流量|过期|官网|套餐|到期|重置|订阅|Anycast|Singapore|Hong Kong|HongKong|Thailand|Bangkok|Canada|Korea|Japan|Australia|Brazil|India|United States|\\bUSA\\b|🇭🇰|🇸🇬|🇹🇭|🇨🇦|🇰🇷|🇯🇵|🇦🇺|🇧🇷|🇮🇳|🇺🇸"

  - name: BACKUP
    type: fallback
    use:
      - sub1
      - sub2
    url: https://api.telegram.org/bot
    interval: 300
    timeout: 4000
    lazy: true
    max-failed-times: 1
    expected-status: 404
    exclude-filter: "(?i)expire|expiring|traffic|remain|剩余|流量|过期|官网|套餐|到期|重置|订阅|Anycast"

  - name: AUTO
    type: fallback
    proxies:
      - FAST
      - BACKUP
    url: https://api.telegram.org/bot
    interval: 30
    timeout: 4000
    lazy: false
    max-failed-times: 1
    expected-status: 404

  - name: SUBSCRIBE
    type: fallback
    proxies:
      - DIRECT
      - AUTO
    # Same host family as provider URLs. Bot API would make DIRECT always fail
    # in RU, so every pull would go through the tunnel.
    url: https://github.com/favicon.ico
    expected-status: 200/204/301/302
    interval: 300
    timeout: 4000
    lazy: false
    max-failed-times: 1

  - name: WHITELIST
    type: select
    use:
      - sub4
      - sub5

  - name: PROXY
    type: select
    proxies:
      - AUTO
      - WHITELIST
      - DIRECT

rules:
  - MATCH,AUTO
